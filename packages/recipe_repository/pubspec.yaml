name: receipe_repository
description: recepie repo
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.7

dependencies:
  equatable: ^2.0.3

  flutter:
    sdk: flutter
  the_meal_db_api_client:
    path: ../the_meal_db_api_client  

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0
